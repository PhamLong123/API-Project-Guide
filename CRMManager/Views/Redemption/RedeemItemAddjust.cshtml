@*@model CRMManager.Models.RedeemItemAdjustsViewModel
    <form method="post">
        <div class="ml-5 mr-5 mt-4">
            <div class="row">
                <div class="col-6">
                    <div class="row">
                        <div class="col-6">
                            <div class="form-group mb-2">
                                <label class="control-label">Chọn quà</label>
                                <select id="drpEmpList" class="form-select" asp-for="ItemID" asp-items="@(new SelectList(Model.RedeemItems, "ID", "ItemName"))">
                                    <option value="">--Select--</option>
                                </select>
                                <input type="hidden" asp-for="ItemID" />
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-6">
                    <a asp-area="" asp-controller="Redemption" asp-action="AddNewRedeemItemAddjust" class="btn btn-success float-right m-1"><i class="far fa-plus-square"></i> Thêm</a>
                </div>
            </div>
            <table id="table-data" class="table table-bordered table-hover table-apptech mt-4 w-100" border="1">
                <thead>
                    <tr style="color: White; background-color: #4267B2;">
                        <td style="width:15%;"><b>Số lượng</b></td>
                        <td style="width:60%;"><b>Mô tả</b></td>
                        <td style="width:25%;"><b>Tùy chọn</b></td>
                    </tr>
                </thead>
                @foreach (var item in Model.RedeemItemAddjusts)
                {
                    <tr id="controlRow" runat="server" class="apptech-pull-up">
                        <td>
                            <div class="d-block w-100 h-100 text-decoration-none text-dark p-10px">
                                @item.Qty
                            </div>
                        </td>
                        <td>
                            <div class="d-block w-100 h-100 text-decoration-none text-dark p-10px">
                                @item.Description
                            </div>
                        </td>
                        <td>
                            <div class="row">
                                <div class="col-6">
                                    <button type="submit" asp-area="" asp-controller="Redemption" asp-action="DeleteRedeemItemAddjust" asp-route-id= class="btn btn-danger m-1"><i class="far fa-trash-alt"></i> Xóa</button>
                                </div>
                            </div>
                        </td>
                    </tr>
                }
            </table>
        </div>
    </form>*@